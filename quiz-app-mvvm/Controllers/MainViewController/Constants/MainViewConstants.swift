import UIKit

extension MainViewController {
    struct MainViewConstants {
        static let logoutButtonImage = "rectangle.lefthalf.inset.filled.arrow.left"
        static let slideButtonImage = "line.3.horizontal"
        static let heightForRow: CGFloat = 100
        static let heightForHeader: CGFloat = 25
    }
}
